public class Monster  extends Entity{
    public Monster(String name, int attack, int defense, int maxHealth, int minDamage, int maxDamage) {
        super(name, attack, defense, maxHealth, minDamage, maxDamage);
    }
}
